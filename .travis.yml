language: c
sudo: required

os:
  - linux
  - osx
  - windows

matrix:
  allow_failures:
    - os: windows

compiler:
  - gcc
  - clang

branches:
  only:
    - master

before_install:
  - 

script:
  - make exodus
  - make release
  - make test

after_success:
  - echo LaGriT successfully compiled
